# TMRW—KeyFlow

ส่วนเสริมสลับ API key อัตโนมัติสำหรับ SillyTavern โดย **tmrw**

KeyFlow จะใช้คีย์เดิมต่อไปจนเกิดข้อผิดพลาดแล้วจึงสลับไปคีย์ถัดไปและลองส่งคำขอเดิมใหม่ 1 ครั้ง รองรับ Google AI Studio ทั้งคีย์ `AIza…` และ `AQ.…` รวมถึง OpenRouter `sk-or-…`

## จุดเด่น

- สลับเฉพาะเมื่อเกิด error ที่เลือกไว้: `401/403`, `429`, `402` และ `5xx` แบบเลือกเปิดได้
- ไม่สลับทุกข้อความ
- รายการคีย์แบบพับเก็บ ค้นหาได้ และแบ่งหน้า 
- ถ้าพบคีย์ 30 อันขึ้นไป จะมีเครื่องมือเก็บคีย์ที่ใช้อยู่ไว้ 1 อันและลบที่เหลือในครั้งเดียว
- ตรวจพบ `allowKeysExposure: true` และมีปุ่มคัดลอกคำสั่งแก้สำหรับ Termux
- เพิ่มหลายคีย์พร้อมกัน เปลี่ยนชื่อ เลือกใช้ ลบ และพักคีย์ที่มีปัญหาได้

## ติดตั้งผ่าน Termux

1. ดาวน์โหลด ZIP ไว้ในโฟลเดอร์ **Download**
2. ปิด SillyTavern ด้วย `Ctrl+C`
3. เปิดไฟล์ `INSTALL-TERMUX.txt` แล้วคัดลอกคำสั่งทั้งก้อนไปวาง
4. เปิด SillyTavern ใหม่ แล้วเข้า **Extensions → TMRW—KeyFlow**

ตัวติดตั้งจะลบโฟลเดอร์ Extension ชื่อ KeyPilot รุ่นเก่าเพื่อป้องกันการทำงานชนกัน แต่จะไม่ลบ API key ที่เก็บอยู่ใน `secrets.json`

## ความปลอดภัย

คีย์จริงจะถูกส่งไปยัง secret manager ของ SillyTavern โดยตรง KeyFlow อ่านเพียง ID ชื่อ สถานะ และค่าที่ถูกปิดบัง ไม่บันทึกคีย์จริงไว้ใน extension settings หรือ log

## ใบอนุญาต

TMRW—KeyFlow เผยแพร่ภายใต้ **TMRW Non-Commercial Attribution License v1.0**

อนุญาตให้นำไปใช้ แก้ไข และแจกจ่ายต่อได้เฉพาะเพื่อวัตถุประสงค์ที่ไม่ใช่
เชิงพาณิชย์ โดยต้องคงเครดิตของ **tmrw** ไว้

ห้ามจำหน่าย สร้างรายได้ หรือนำไปใช้เป็นส่วนหนึ่งของสินค้าและบริการที่มี
การเรียกเก็บเงิน

โปรดอ่านเงื่อนไขฉบับเต็มในไฟล์ [LICENSE](LICENSE)


เขียนทำไมหน้านี้ มีคนอ่านด้วยเหรอ
ยินดีด้วย คุณอ่านมาถึงบรรทัดสุดท้าย รับดาวหนึ่งดวงไปเลยคนเก่ง